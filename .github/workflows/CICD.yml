name: CI

on:
 workflow_dispatch

 env:
  DOCKER_IMAGE: subramsr/python_image
  DOCKER_USERNAME: ${{var.DOCKER_NAME}}
  DOCKER_PASSWORD: ${{var.DOCKER_PASSWD}}

jobs:
 code-checkout:
  runs-on: ubuntu-latest
  steps:
   - name: Checkout
     uses: actions/checkout@v5.0.0
